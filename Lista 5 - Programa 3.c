#include <stdio.h>
#include <stdlib.h>

int main()
{
    int vet[10], i;
    for(i=0; i<=10; i++)
    {
        printf("Digite o %i numero: ");
        scanf("%i", &vet[i]);
    }
        printf("\tSelecione sua opcao:\n\n-Exibir os numeros em ordem crescente.\n-Armazenar mais um numero.\n-Sair.\n\nDigite: ");
        scanf("%i", &i);
        switch (i)
        {
        case 1:
            {

            }
        }
    return 0;
}
